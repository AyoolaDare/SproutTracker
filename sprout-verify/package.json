{
  "name": "sprout-verify",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Public Sprout Track invoice and receipt verification site for Cloudflare Pages.",
  "scripts": {
    "check": "node --check functions/api/verify/[receipt_id].js && node --check functions/api/health.js && node --check public/app.js && node --check src/index.js"
  },
  "engines": {
    "node": ">=20"
  }
}
